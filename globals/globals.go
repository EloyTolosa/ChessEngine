package globals

const (
	// App constantsmaxFPS = 60
	MaxFPS       = 6
	WindowWidth  = 800
	WindowHeight = 800

	TableDim = 8

	CWidth  = int(WindowWidth / TableDim)
	CHeight = int(WindowHeight / TableDim)
)
